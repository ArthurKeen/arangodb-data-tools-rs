# Rust / Cargo
/target
**/target
**/*.rs.bk
*.pdb
# Cargo.lock is intentionally tracked because this workspace ships a binary (the CLI).

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Agent / tooling local config
.claude/
.cursor/

# Local env / secrets
.env
.env.*
*.pem
*.key

# Test / scratch output
/tmp
/dump
/scratch
