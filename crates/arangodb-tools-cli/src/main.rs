//! `arangox` command-line entry point.
//!
//! Subcommands (`import`, `export`, `dump`, `restore`, `rdf`) will be wired to
//! the library crates as they land. See `docs/IMPLEMENTATION_PLAN.md`.

fn main() {
    println!(
        "arangox: ArangoDB data tools (pre-alpha). Commands are not implemented yet; \
         see docs/IMPLEMENTATION_PLAN.md."
    );
}
